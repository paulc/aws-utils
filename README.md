# aws-utils
AWS Utils

```
Usage: gist.py [OPTIONS] [FILES]...

Options:
  -d, --description TEXT  Gist description  [required]
  -p, --private           Private gist
  -s, --short             Short URL
  -n, --name TEXT         File name (stdin)
  -b, --base64            Base64 encode file(s)
  -c, --config PATH       Account config
  -a, --account TEXT      GitHub account
  -t, --token TEXT        GitHub token
  --help                  Show this message and exit.
```

```
Usage: lightsail.py [OPTIONS] COMMAND [ARGS]...

Options:
  --help  Show this message and exit.

Commands:
  blueprints
  bundles
  cmd
  keys
  ls
  new
  ports
  rm
  ssh
```

```
Usage: ec2.py [OPTIONS] COMMAND [ARGS]...

Options:
  --help  Show this message and exit.

Commands:
  delsg
  editsg
  listsg
  ls
  newsg
```
